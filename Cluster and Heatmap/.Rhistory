###Load the library
library("pheatmap")
###Load the data
data<-read.csv(file.choose()) #without cluster
head(data)
data$x<-as.character(x=data$Gene.Name)
rownames(data)<-c(data$x)
head(data)
rownames(data)<-c(data$Gene.Name)
###Load the data
data<-read.csv(file.choose()) #without cluster
head(data)
rownames(data)<-c(data$Gene.Name)
###Load the data
data<-read.csv(file.choose()) #without cluster
head(data)
str(data)
rownames(data)<-c(data$Gene.Name)
head(data)
newdata<-newdata [ , -1]
newdata<-data [ , -1]
head(newdata)
matx <- scale(newdata)
heatmap(matx, scale = "row")
###Draw Heatmap with Row & Column Clusters (Euclidean", "ward.D2", method )
pheatmap(matx,
color=colorRampPalette(c("navy", "white", "red"))(50))
pheatmap(matx,
clustering_distance_rows = "euclidean",
clustering_distance_cols = "euclidean", clustering_method = "ward.D2",
cutree_rows = 3, cutree_cols =3,
legend = T,
fontsize = 11,
color=colorRampPalette(c("navy", "white", "red"))(50))
###Load the library
library("pheatmap")
###Load the data
data<-read.csv(file.choose()) #without cluster
head(data)
str(data)
data$x<-as.character(x=data$Gene.Name)
str(data)
head(data)
rownames(data)<-c(data$Gene.Name)
head(data)
newdata<-data [ , -1]
head(newdata)
matx <- scale(newdata)
matx
?matx
??matx
?scale
heatmap(matx, scale = "row")
View(matx)
head(data)
?pheatmap
###Load the library
library("pheatmap")
?pheatmap
###Load the library
library("pheatmap")
###Load the data
data<-read.csv(file.choose()) #without cluster
###Load the data
data<-read.csv(file.choose()) #without cluster
head(data)
str(data)
rownames(data)<-c(data$Gene.Name)
head(data)
rownames(data)<-c(data$Gene.name)
head(data)
newdata<-data [ , -1]
head(newdata)
newdata<-data [ , -2]
head(newdata)
matx <- scale(newdata)
View (newdata)
newdata<-newdata [ , -1]
head(newdata)
matx <- scale(newdata)
heatmap(matx, scale = "row")
# Set up the TIFF device with desired DPI
tiff(filename = file_name, width = 6 * dpi, height = 4 * dpi, res = dpi)
# Set up the TIFF device with desired DPI
tiff(filename = adil, width = 6 * 600, height = 4 * 600, res = 600)
# Define the file name and desired DPI
file_name <- "plot_image.tiff"
dpi <- 300
# Set up the TIFF device with desired DPI
tiff(filename = file_name, width = 6 * dpi, height = 4 * dpi, res = dpi)
pheatmap(matx,
clustering_distance_rows = "euclidean",
clustering_distance_cols = "euclidean", clustering_method = "ward.D2",
cutree_rows = 3, cutree_cols =3, angle_col = 45, angle_row = 45,
legend = T,
fontsize = 11,
color=colorRampPalette(c("navy", "white", "red"))(50))
pheatmap(matx,
clustering_distance_rows = "euclidean",
clustering_distance_cols = "euclidean", clustering_method = "ward.D2",
cutree_rows = 3, cutree_cols =3, angle_row = 45,
legend = T,
fontsize = 11,
color=colorRampPalette(c("navy", "white", "red"))(50))
pheatmap(matx,
clustering_distance_rows = "euclidean",
clustering_distance_cols = "euclidean", clustering_method = "ward.D2",
cutree_rows = 3, cutree_cols =3, labels_row=expression(italic(rownames)),
legend = T,
fontsize = 11,
color=colorRampPalette(c("navy", "white", "red"))(50))
pheatmap(matx,
clustering_distance_rows = "euclidean",
clustering_distance_cols = "euclidean", clustering_method = "ward.D2",
cutree_rows = 3, cutree_cols =3, labels_row=expression(italic(c(CmTH01, CmTH02))),
legend = T,
fontsize = 11,
color=colorRampPalette(c("navy", "white", "red"))(50))
pheatmap(matx,
clustering_distance_rows = "euclidean",
clustering_distance_cols = "euclidean", clustering_method = "ward.D2",
cutree_rows = 3, cutree_cols =3, labels_row=expression(italic(c("CmTH01", "CmTH02"))),
legend = T,
fontsize = 11,
color=colorRampPalette(c("navy", "white", "red"))(50))
pheatmap(matx,
clustering_distance_rows = "euclidean",
clustering_distance_cols = "euclidean", clustering_method = "ward.D2",
cutree_rows = 3, cutree_cols =3, labels_row=expression(italic(c("CmTH01", "CmTH02"))),
legend = T,
fontsize = 11,
color=colorRampPalette(c("navy", "white", "red"))(50))
# Turn off the device to save the file
dev.off()
###Load the library
library("pheatmap")
###Load the data
data<-read.csv(file.choose()) #without cluster
head(data)
str(data)
rownames(data)<-c(data$Genotype)
head(data)
newdata<-data [ , -1]
head(newdata)
matx <- scale(newdata)
heatmap(matx, scale = "row")
###Draw Heatmap with Row & Column Clusters (Euclidean", "ward.D2", method )
pheatmap(matx,
color=colorRampPalette(c("navy", "white", "red"))(50))
pheatmap(matx,
clustering_distance_rows = "euclidean",
clustering_distance_cols = "euclidean", clustering_method = "ward.D2",
cutree_rows = 3, cutree_cols =3, labels_row=expression(italic(c("CmTH01", "CmTH02"))),
legend = T,
fontsize = 11,
color=colorRampPalette(c("navy", "white", "red"))(50))
pheatmap(matx,
color=colorRampPalette(c("navy", "white", "red"))(50))
